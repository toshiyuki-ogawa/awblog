{
  "name": "awblog-base",
  "version": "0.0.1",
  "description": "basic module",
  "license": "Apache-2.0",
  "author": "Toshiyuki Ogawa",
  "type": "module",
  "exports": {
    "types": "./dist/index.d.ts",
    "import" : "./dist/index.js"
  },
  "scripts": {
    "tsc-check": "tsc --noEmit",
    "tsc-dist": "tsc --declaration --outDir dist --rootDir ./src",
    "tsc-decl": "tsc --declaration --emitDeclarationOnly --outDir dist --rootDir ./src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
